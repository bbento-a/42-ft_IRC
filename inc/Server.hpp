#ifndef SERVER_HPP
#define SERVER_HPP

// Notes from metro thinking time aahh (it means this needs reviewing)
/* 
class Server
{
	private:
	Clients[] //connected clients to the sv
	Channels[] //existing channels at sv
	
	public:
	//OCF
	ServerStart() // check everything for start up
	ServerRuntime() //loop of connections
	RegisterClient() // check client infos to link them to the sv
	ConnectClient() //maybe?
};

class Channel
{
	private:
	Clients[] // these are the ones connected to the channel
	// should the ops be a derived from client?
	//sm more important cmds (like ban, kick)

	public:
	//OCF
	//most cmds (like join, msg)
};
*/

#endif